# Test overrides — used in CI for E2E tests
services:
  postgres:
    environment:
      POSTGRES_DB: osint_test
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test

  api:
    environment:
      APP_ENV: test
      POSTGRES_HOST: postgres
      POSTGRES_PORT: 5432
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: osint_test
      REDIS_URL: redis://redis:6379/1
      JWT_SECRET_KEY: test-secret-key
      DEBUG: "true"
